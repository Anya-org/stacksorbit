# StacksOrbit Requirements
# Python 3.8+ required

# Core dependencies for enhanced CLI functionality
python-dotenv>=1.2.2
toml>=0.10.2
requests>=2.34.2
pyyaml>=6.0.3
colorama>=0.4.6
textual>=8.2.7

# GUI dependencies (included with Python)
# tkinter (included with Python)
# pathlib (included with Python 3.4+)
# subprocess (standard library)
# threading (standard library)
# queue (standard library)

# Blockchain interaction
# Note: Node.js dependencies handled by package.json

# Development dependencies (optional)
pytest>=9.0.3
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-asyncio
black>=26.5.1
pylint>=4.0.5
mypy>=2.1.0

# Additional tools for enhanced deployment
psutil>=5.9.0  # System monitoring
websocket-client>=1.6.0  # Real-time monitoring
